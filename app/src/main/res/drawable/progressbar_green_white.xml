<layer-list xmlns:android="http://schemas.android.com/apk/res/android">



    <!-- Background -->
    <item android:id="@android:id/background">
        <inset
            android:left="2dp"
            android:top="2dp"
            android:right="2dp"
            android:bottom="2dp">
            <shape android:shape="rectangle">
                <solid android:color="@color/white"/>
                <corners android:radius="18dp"/>
            </shape>
        </inset>
    </item>

    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <inset
                android:left="2dp"
                android:top="2dp"
                android:right="2dp"
                android:bottom="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/Paolo_Veronese_Green"/>
                    <corners android:radius="18dp"/>
                </shape>
            </inset>
        </clip>
    </item>

    <!-- Stroke -->
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="2dp" android:color="@color/Nautical" />
            <corners android:radius="20dp"/>
        </shape>
    </item>
</layer-list>
